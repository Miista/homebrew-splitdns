/sd
/shd
/mirage
/splitdns

# Never commit built binaries — use goreleaser / deploy.sh / apt.
*-linux-amd64
*-linux-arm64

# Local integration fixture (real deployment layout) — never commit.
/testrepo/

# Generated by tools/genman (release workflow regenerates it).
/man/

# Never commit secrets — SSH private keys belong in 1Password / ~/.ssh, not here.
id_*
!id_*.pub
*.pem
*.key

# macOS cruft.
.DS_Store
